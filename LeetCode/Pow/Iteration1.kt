import kotlin.math.pow

class Solution {
    fun myPow(x: Double, n: Int) = x.pow(n)
}
/**
 * This answer is not valid (It passes the test cases).
 * I don't why I couldn't solve this damn problem.
 * */